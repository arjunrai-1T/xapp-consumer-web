import { createTheme } from '@mui/material/styles';

// Create the theme with Ubuntu as the default font
const theme = createTheme({
  typography: {
    fontFamily: '"Ubuntu", sans-serif',
  },
});

export default theme;
